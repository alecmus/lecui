Below are step-\/by-\/step instructions on how to use the library to build a Windows gui app in C++. This file will be revised from time to time.\hypertarget{md__t_u_t_o_r_i_a_l_autotoc_md17}{}\doxysubsection{Inherit from the form base class}\label{md__t_u_t_o_r_i_a_l_autotoc_md17}
To make a form we need to inherit from the form base class and since there is no default constructor we need to explicitly call one of the form\textquotesingle{}s constructors. The one that we shall use here set\textquotesingle{}s the form\textquotesingle{}s caption (the text that is displayed on the top left corner of the form).


\begin{DoxyCode}{0}
\DoxyCodeLine{ \{C++\}}
\DoxyCodeLine{\#include <liblec/lecui/form.h>}
\DoxyCodeLine{using namespace liblec;}
\DoxyCodeLine{}
\DoxyCodeLine{class sample\_form : public lecui::form \{}
\DoxyCodeLine{public:}
\DoxyCodeLine{  sample\_form() :}
\DoxyCodeLine{    form("{}Sample Form"{}) \{\}}
\DoxyCodeLine{\};}

\end{DoxyCode}
\hypertarget{md__t_u_t_o_r_i_a_l_autotoc_md18}{}\doxysubsection{Override the on\+\_\+layout virtual method}\label{md__t_u_t_o_r_i_a_l_autotoc_md18}
The on\+\_\+layout method is where we add content to the form.


\begin{DoxyCode}{0}
\DoxyCodeLine{ \{C++\}}
\DoxyCodeLine{\#include <liblec/lecui/form.h>}
\DoxyCodeLine{using namespace liblec;}
\DoxyCodeLine{}
\DoxyCodeLine{class sample\_form : public lecui::form \{}
\DoxyCodeLine{  bool on\_layout(std::string\& error) override \{}
\DoxyCodeLine{    return true;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{}
\DoxyCodeLine{public:}
\DoxyCodeLine{  sample\_form() :}
\DoxyCodeLine{    form("{}Sample Form"{}) \{\}}
\DoxyCodeLine{\};}

\end{DoxyCode}


Things to note\+:
\begin{DoxyEnumerate}
\item The on\+\_\+layout method takes only one parameter, a string. If an error occurs it is written back to this string.
\item The on\+\_\+layout() method returns a boolean. If the method is successful true is returned and if not false is returned (in which case error information must be written to the parameter we just mentioned).
\end{DoxyEnumerate}\hypertarget{md__t_u_t_o_r_i_a_l_autotoc_md19}{}\doxysubsection{Add a page to the form and show it}\label{md__t_u_t_o_r_i_a_l_autotoc_md19}
Content can only be added to containers, and only two containers can be added directly to the form\+: a \href{https://github.com/alecmus/lecui/blob/master/containers/page.h}{\texttt{ page}} and a \href{https://github.com/alecmus/lecui/blob/master/containers/status_pane.h}{\texttt{ status\+\_\+pane}}. To add a page we need the page\+\_\+management class. It is common for the page\+\_\+management class to be used in various places in the class and so it is recommended that we add it as an in-\/class object.


\begin{DoxyCode}{0}
\DoxyCodeLine{ \{C++\}}
\DoxyCodeLine{\#include <liblec/lecui/containers/page.h>}
\DoxyCodeLine{using namespace liblec;}
\DoxyCodeLine{}
\DoxyCodeLine{class sample\_form : public lecui::form \{}
\DoxyCodeLine{  lecui::page\_manager page\_man\_\{ *this \};}
\DoxyCodeLine{}
\DoxyCodeLine{  bool on\_layout(std::string\& error) override \{}
\DoxyCodeLine{    auto\& home = page\_man\_.add("{}home"{});}
\DoxyCodeLine{    page\_man\_.show("{}home"{});}
\DoxyCodeLine{    return true;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{}
\DoxyCodeLine{public:}
\DoxyCodeLine{  sample\_form() :}
\DoxyCodeLine{    form("{}Sample Form"{}) \{\}}
\DoxyCodeLine{\};}

\end{DoxyCode}


Things to note\+:
\begin{DoxyEnumerate}
\item The add() method takes only one parameter, the alias of the page. Every page MUST have an alias and it must be unique. This is how the page is distinguished from others.
\item The add() method returns a reference to the page. We can then use this reference to add widgets to the page.
\item The show() method takes one parameter, the alias of the page to show when the form is displayed. Several pages can be created but only one can be displayed at a time.
\item Once we include any other lecui header other than $<$.../form.h$>$ we no longer need $<$.../form.h$>$ because it is included in every other header.
\end{DoxyEnumerate}\hypertarget{md__t_u_t_o_r_i_a_l_autotoc_md20}{}\doxysubsection{Add a widget to the page}\label{md__t_u_t_o_r_i_a_l_autotoc_md20}
Widgets can be added to any container using the widget\textquotesingle{}s builder class. We will add a simple label to the page using the label\+\_\+builder.


\begin{DoxyCode}{0}
\DoxyCodeLine{ \{C++\}}
\DoxyCodeLine{\#include <liblec/lecui/containers/page.h>}
\DoxyCodeLine{\#include <liblec/lecui/widgets/label.h>}
\DoxyCodeLine{using namespace liblec;}
\DoxyCodeLine{}
\DoxyCodeLine{class sample\_form : public lecui::form \{}
\DoxyCodeLine{  lecui::page\_manager page\_man\_\{ *this \};}
\DoxyCodeLine{}
\DoxyCodeLine{  bool on\_layout(std::string\& error) override \{}
\DoxyCodeLine{    auto\& home = page\_man\_.add("{}home"{});}
\DoxyCodeLine{}
\DoxyCodeLine{    lecui::widgets::label\_builder label(home);}
\DoxyCodeLine{    label.specs()}
\DoxyCodeLine{      .text("{}This is a simple label"{})}
\DoxyCodeLine{      .rect(\{ 10.f, home.size().width -\/ 10.f, 10.f, 30.f \});}
\DoxyCodeLine{}
\DoxyCodeLine{    page\_man\_.show("{}home"{});}
\DoxyCodeLine{    return true;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{}
\DoxyCodeLine{public:}
\DoxyCodeLine{  sample\_form() :}
\DoxyCodeLine{    form("{}Sample Form"{}) \{\}}
\DoxyCodeLine{\};}

\end{DoxyCode}


Things to note\+:
\begin{DoxyEnumerate}
\item The specs() method returns a reference to the widget specifications. For more terse code we can optionally use the () operator overload as follows 
\begin{DoxyCode}{0}
\DoxyCodeLine{label()}
\DoxyCodeLine{  .text(...)}
\DoxyCodeLine{  .rect(...)}

\end{DoxyCode}

\item The rect() method is used to specify the coordinates of the widget. We use it to specify the coordinates in this order\+: left, right, top, bottom. Optionally we can use chaining as follows\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{label()}
\DoxyCodeLine{  .text(...)}
\DoxyCodeLine{  .rect().left(10.f).right(home.size().width -\/ 10.f).top(10.f).bottom(30.f);}

\end{DoxyCode}

\end{DoxyEnumerate}\hypertarget{md__t_u_t_o_r_i_a_l_autotoc_md21}{}\doxysection{Make an instance of the class and show the form}\label{md__t_u_t_o_r_i_a_l_autotoc_md21}
We can instantiate our class and call the show() method as follows to display the form\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{ \{C++\}}
\DoxyCodeLine{int main() \{}
\DoxyCodeLine{    sample\_form form\_;}
\DoxyCodeLine{    std::string error;}
\DoxyCodeLine{    if (!form\_.show(error))}
\DoxyCodeLine{        form\_.message(error);}
\DoxyCodeLine{    return 0;}
\DoxyCodeLine{\}}

\end{DoxyCode}


Things to note\+:
\begin{DoxyEnumerate}
\item The show() method takes only one parameter, a string. If an error occurs it is written back to this string.
\item The show() method returns a boolean. If the method is successful true is returned and if not false is returned (in which case error information will be available in the parameter we just mentioned).
\item The message() method is used to display a message box to the user. It\textquotesingle{}s a simple message box with an Ok button. 
\end{DoxyEnumerate}